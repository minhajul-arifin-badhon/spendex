import type { NextConfig } from "next";

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ["cdn-icons-png.flaticon.com"], // Add the domain of the external URL here
	},
};

export default nextConfig;
